/** @type {import('next').NextConfig} */

const path = require('path');

const nextConfig = {
    images: {
        domains: ['commerce-project.s3.ap-northeast-2.amazonaws.com']
    },
}

module.exports = nextConfig
